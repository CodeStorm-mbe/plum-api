# Django
SECRET_KEY=*&$$@$sg#p2i4g4fet^*0@swwb^_q&5l4h43dfg92kl0c*5i4)
DEBUG=False  # Désactivé pour production
ALLOWED_HOSTS=localhost,127.0.0.1,*.onrender.com  # Inclut le domaine Render

# Base de données (option SQLite)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=/app/db.sqlite3  # Chemin dans le conteneur
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Base de données (option PostgreSQL, activer si utilisé)
# DATABASE_URL sera défini par Render si tu crées une DB PostgreSQL
# POSTGRES_PASSWORD=postgres
# POSTGRES_USER=postgres
# POSTGRES_DB=plum_db

# Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend  # SMTP pour production
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=ton-email@gmail.com  # Remplace par ton email
EMAIL_HOST_PASSWORD=ton-mot-de-passe-app  # Remplace par un mot de passe d'application Gmail
DEFAULT_FROM_EMAIL=noreply@plumclassifier.com

# Frontend
FRONTEND_URL=https://ton-frontend.onrender.com  # Remplace par l'URL de ton frontend (ou supprime si pas utilisé)

# Redis (désactivé pour Render gratuit, utiliser Upstash si besoin)
# REDIS_URL=redis://external-service  # Décommente si tu configures Upstash

# Celery (désactivé pour Render gratuit, activer si Redis configuré)
# CELERY_BROKER_URL=redis://external-service
# CELERY_RESULT_BACKEND=redis://external-service

# CORS
CORS_ALLOWED_ORIGINS=https://ton-frontend.onrender.com  # Remplace par ton frontend (ou supprime si pas utilisé)